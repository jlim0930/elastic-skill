services:
  mcp-docs-server:
    build: https://github.com/jlim0930/mcp-server.git
    container_name: mcp-docs-app
    ports:
      - "8888:8000"
    environment:
      # Pass your comma-separated list of documentation sites here
      - DOC_SITES=https://www.elastic.co/docs,https://www.elastic.co/docs/api,https://github.com/elastic
      # Enable auto-indexing on startup
      - AUTO_INDEX=true
    volumes:
      # This maps a local folder to the container's DB path for persistent indexing
      - ./mcp_data:/app/chroma_db
    command: ["sse"]
    restart: unless-stopped
    # Required for Crawl4AI to have enough shared memory for the headless browser
    shm_size: '2gb'
